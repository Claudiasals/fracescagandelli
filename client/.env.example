# Produzione (Netlify): URL completo del backend Render
# VITE_API_URL=https://francescagandelli.onrender.com/api

# Sviluppo: di default non serve — `npm run dev` usa il proxy /api → localhost:5000.
# Usa questa riga solo se NON passi dal proxy Vite (es. build preview su telefono):
# VITE_API_URL=http://192.168.0.251:5000/api
